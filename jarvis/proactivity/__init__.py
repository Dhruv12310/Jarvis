"""Jarvis Core (Phase 5): reflection + user model (5a); engine + feedback (5b/5c).

THE law (Core §8): usefulness, never engagement. The reflection trigger, context aggregation, the
user-model confidence math, and (5b/5c) ranking + feedback are DETERMINISTIC code; the LLM only
synthesizes (grounded on a code-built context) and phrases. Everything local; the user model is
inspectable and the user's. Nothing about the user's behavior leaves the machine.
"""
